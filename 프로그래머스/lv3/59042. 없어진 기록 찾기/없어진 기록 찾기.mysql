SELECT AO.ANIMAL_ID, AO.NAME
FROM ANIMAL_OUTS AO
LEFT OUTER JOIN ANIMAL_INS AI
ON AO.ANIMAL_ID = AI.ANIMAL_ID
WHERE AI.ANIMAL_ID IS NULL;

-- LEFT OUTER JOIN을 사용한 후 교집합 삭제 => ANIMAL_OUTS과 ANIMAL_INS의 차집합 SQL


