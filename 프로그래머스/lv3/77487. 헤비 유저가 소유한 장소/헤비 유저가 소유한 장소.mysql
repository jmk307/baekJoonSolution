SELECT ID, NAME, HOST_ID
FROM PLACES A
WHERE (
SELECT COUNT(HOST_ID)
FROM PLACES B
WHERE A.HOST_ID = B.HOST_ID
) > 1;



# SELECT HOST_ID, COUNT(HOST_ID)
# FROM PLACES
# WHERE COUNT(HOST_ID) > 1
# GROUP BY HOST_ID

# SELECT * FROM
# (SELECT COUNT(HOST_ID) B
# FROM PLACES
# GROUP BY HOST_ID) A

# SELECT COUNT(HOST_ID)
# FROM PLACES
# GROUP BY HOST_ID

